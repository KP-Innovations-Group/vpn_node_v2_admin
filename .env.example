# VPN Node v2 Admin Panel environment

# Base URL of the VPN node management API.
# For local dev the Vite proxy will forward /api, /health and /swagger to this origin.
VITE_API_ORIGIN=http://localhost:8080

# Optional: a friendly label shown in the app shell.
VITE_NODE_LABEL=Local Node
